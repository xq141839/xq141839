name: Update Cultivation Rank

on:
  schedule:
    - cron: '0 16 * * *' # 每天北京时间 0:00 运行 (UTC 16:00)
  workflow_dispatch: # 允许你手动点击按钮运行

jobs:
  update-rank:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # 重要：给予机器人修改仓库文件的权限
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: pip install requests

      - name: Run script
        run: python main.py

      - name: Commit and Push
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add README.md
          # 如果没有变化就不提交，避免报错
          git diff --quiet && git diff --staged --quiet || (git commit -m "🔥 斗气化翼：修炼等级更新" && git push)
